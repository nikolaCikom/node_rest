<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Companies</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  </head>
  <body>
    <header><%- include('./nav/navbar') %></header>
    <div class="container mt-5 mb-5">
      <div class="container">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title"><%= company.name %></h5>
            <!-- <img src="<%= company.image %> /> -->
            <img src="/images/<%= company.image %>" class="w-100" />
            <p class="card-text"><%= company.description %></p>
            <a class="btn btn-primary" href="/updatecompany/<%= company.id %>">Edit Company</a>
            <button id="deleteCompanyButton" data-companyId="<%= company.id %>" class="btn btn-danger">Delete Company</button>
          </div>
        </div>
      </div>
    </div>

    <%- include('./footer/footer') %>

    <!-- Uvoz Bootstrap JavaScript-a (Popper.js i Bootstrap.js) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="../js/company.js"></script>
  </body>
</html>
