<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NodeJs Index</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  </head>
  <body>
    <header><%- include('./nav/navbar') %></header>

    <main class="container mt-5">
      <h1>Dobrodošli na Moj Sajt</h1>
      <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Vivamus vitae justo eu nulla lobortis ultrices.</p>
      <div class="mb-4">
        <a class="btn btn-primary" href="#">Dodaj Usera</a>
        <a class="btn btn-primary" href="/createcompany">Dodaj Kompaniju</a>
      </div>
      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Username</th>
            <th>Email</th>
            <th>Company Name</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach((user) => { %>
          <tr>
            <td><%= user.name %></td>
            <td><%= user.username %></td>
            <td><%= user.email %></td>
            <td><%= user.company ? user.company.name : '' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </main>

    <%- include('./footer/footer') %>

    <!-- Uvoz Bootstrap JavaScript-a (Popper.js i Bootstrap.js) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
